local eps in MAD.constant

local twiss_list = {
    beta11 = eps,
    beta22 = eps,
    mu1    = eps,
    mu2    = eps,
    alfa11 = eps,
    alfa22 = eps,
}
local info = {
    ["twiss_n_m.tfs"] = twiss_list,
    ["twiss_n_x.tfs"] = twiss_list,
}
local ref_files = {
    ["twiss_n_m.tfs"] = {"twiss_x.ref"},
    ["twiss_n_x.tfs"] = {"twiss_x.ref"},
}
return info, ref_files