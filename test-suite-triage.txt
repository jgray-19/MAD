MAD test-suite triage
Date: 2026-03-13

Commands run
- From `tests/utests`: `../../mad all.mad`
- From `tests/utests`: `../../mad all.mad -e -v`
- From `tests/utests`: `../../mad all.mad -e -v -x TestMatch`
- From `tests/utests`: additional `-e -v` passes with targeted `-x` exclusions to walk the error ladder
- Targeted reruns with `-e -v` for failing tests as fixes were applied
- Rebuilds from `src`: `make -f Makefile`

Current status
- Current error-only pass:
  - `../../mad all.mad -e -v`
  - `Ran 886 tests (605K asserts) in 1.354 seconds`
  - `885 successes, 1 error`
  - first error is now `TestMTable.testWriteCol`
- With `TestMTable.testWriteCol` excluded:
  - next hard error is `TestMatrixLapack.testPcacnd` (`SIGSEGV`)
- With `TestMatrixFun.testMap`, `TestMatrixFun.testMap2`, and `TestMatrixLapack.testPcacnd` excluded:
  - the next non-crash error is `TestSequence.testInitElemFromPos`
- All earlier `geomap`, `cofind`, `logrange`, and `mtable` blockers seen at the start of this pass are now cleared.
- With `TestMatch` excluded, the suite gets through `mtable` and into `matrix`.
- Current skip-`match` blocker remains:
  - `TestMatrixFun.testMap`
  - `TestMatrixFun.testMap2`
  - `TestMatrixLapack.testPcacnd` segfaults immediately afterward
- With `TestMatch`, `TestMatrixFun.testMap`, `TestMatrixFun.testMap2`, and `TestMatrixLapack.testPcacnd` excluded, the suite reaches `sequence`.
- Current deeper skip result:
  - `Ran 1454 tests (800K asserts) in 9.026 seconds`
  - `1445 successes, 8 failures, 1 error, 44 non-selected`

What I fixed
1. `src/madl_survey.mad`
   - Imported `math.min` alongside `math.max`.
   - This fixed the strict-global runtime failure in the survey/geomap path.

2. `src/madl_utest.lua`
   - Restored table-aware `assertAllAlmostEquals` behavior.
   - This was an API regression relative to the checked-in tests.
   - Documented in `src/help/madh_utest.mad`.

3. `cofind` tests and docs
   - Updated `tests/utests/cofind.mad` to the current contract:
     - compare phase-space coordinates only for particle results
     - use `cofind.cotol`
   - Documented the current metadata/contract in:
     - `doc/refman/source/mad_cmd_cofind.rst`
     - `src/help/madh_cofind.mad`

4. `mtable` get/select/remove/read bugs
   - `src/madl_mtable.mad`
   - Fixed:
     - `get_cel` now returns `nil` for invalid row lookups instead of indexing through `nil`
     - packed-form `foreach`/`select`/`deselect`/`filter` now extract `range` correctly
     - packed-form `default="none"` now behaves as documented
     - `remrow` now shifts vector-backed columns from the correct index
     - header read validation now catches unmatched string/table lengths cleanly
     - `add_row` now rejects only the root `mtable` object and ownerless root views, avoiding a nil-column crash while leaving the removed readonly behavior unchanged

5. `mtable` API/test/doc cleanup around old behavior
   - Reverted the temporary `copy_mm` owner-copy change in code.
   - Updated the stale `mtable` copy test to the current documented API.
   - Updated `doc/refman/source/mad_gen_mtable.rst` to document:
     - the current `mtable:copy([name])` contract
     - the removal of the old `owner` creation/copy option
     - the post-2022 `set_readonly` behavior, where object attributes remain readonly but row/column data editing is still allowed
   - Updated stale readonly tests in `tests/utests/mtable.mad` to the current contract confirmed by history (`b8856dde`, `remove readonly protection at column/row level`).

6. Old range-literal usage in tests
   - Replaced stale `a..b` range syntax in `tests/utests/mtable.mad` with explicit `range(a, b[, step])`.
   - This included iterator/range tests and stored header/data range values.

7. `logrange` stale expectations
   - Updated `tests/utests/logrange.mad` to the current implementation:
     - constructor acceptance for tiny positive steps
     - `nlogrange(...):bounds()` semantics
     - `nlogrange(...):value()` endpoint behavior
     - canonical `tostring()` formatting via `num2str`
   - No code change was needed here; the tests were out of sync with the current range API.

8. `kpairs` column tests
   - Updated `tests/utests/mtable.mad` so column `kpairs` checks only run on mappable columns.
   - Specialized numeric columns are currently raw vectors/cdata and do not satisfy the `MAD.utility.kpairs` contract.

9. stale test imports/helpers
   - Updated `tests/utests/mtable.mad` to use `math.random` and `math.min`.
   - `MAD.gmath` does not currently export `random`, and the `min` lookup in `assertFiles()` only surfaced once the numeric-comparison path was exercised in the full run.

10. `mtable` write fixtures
   - Updated `tests/utests/mtable_ref/tbl-write.tfs` to the current writer output.
   - Updated the other write fixtures (`tbl-name*`, `tbl-dict*`, `tbl-col*`, `tbl-header*`, `tbl-novector*`, `tbl-rightshft*`, `tbl-stream`) for the post-`39c69d43` behavior where the base mtable no longer has a default `title`.

11. Formatting/denormal-only test adjustments
   - `tests/utests/complex.mad`: relaxed `polar()` assertions around subnormal zero-adjacent values.
   - `tests/utests/complex.mad`: restored `option.numfmt` after `testTocomplex()`, which fixed the later full-run formatting leaks into `mtable` and `matrix` text-output tests.
   - `tests/utests/gmath.mad`: accepted the current `proj(nan)` zero-formatting.

12. `object` stale test/API cleanup
   - `tests/utests/object.mad`
   - Replaced deep-equality assertions with identity assertions (`assertIs`) for methods whose contract is to return `self`.
   - Updated stale test imports from `MAD.operator` to the current operator tables (`MAD.lfun.operator` / `MAD.gfunc`).
   - Updated stale override-policy tests for `set_variables()` and `set_methods()` to the current post-`973840df` behavior.
   - Documented the current `set_methods(set, [override])` behavior in `doc/refman/source/mad_gen_object.rst`:
     - overriding ordinary methods is allowed
     - `override ~= true` only protects private `__*` methods
     - classes can update their methods

13. `range` floating-point edge tests
   - `tests/utests/range.mad`
   - Relaxed three exact assertions that depended on signed-zero/subnormal formatting or exact last-step reconstruction:
     - `nrange(1, 0, huge).step`
     - `nrange(1, 10, huge):value(huge-1)`
     - `nrange(1, 20, huge):bounds()`
   - No runtime code change was made; the current implementation is numerically consistent but not bit-identical to the older expectations.

14. `sequence` copy/cycle stale API cleanup
   - `tests/utests/sequence.mad`
   - Updated stale `view:copy(..., owner)` expectations to the current `copy([name])` API.
   - Updated `doc/refman/source/mad_gen_sequence.rst` for the current copy contract:
     - removed the old `owner` argument
     - documented that copying a view yields another view
   - Updated `sequence:cycle()` tests to the current marker-only contract introduced in history (`c0f7df23`, “impose seq cycle to be a marker, like in MAD-X”).
   - Documented the marker-only `cycle(a)` requirement in `doc/refman/source/mad_gen_sequence.rst`.

15. `match` stale helper errors
   - `tests/utests/match.mad`
   - Removed the four orphaned tests that still called deleted helper functions:
     - `TestMatch.testMatchFuncJAC6`
     - `TestMatch.testMatchFuncLMD6`
     - `TestMatch.testMatchFuncMMA6`
     - `TestMatch.testMatchFuncSPQ7`
   - This was based on the inline history already present in the file:
     - `testMatchFunc6` / `testMatchFunc7` were removed/renamed in `aca10ec8`
     - the tests were left behind and only produced missing-global errors

16. plot temp-folder cleanup error
   - `src/madl_plot.mad`
   - Fixed `clean_tmp()` so it only deletes `TMP_MAD_PLOT/` when:
     - each recognized temp file was actually removed
     - there are no unexpected leftover entries in the folder
   - This fixed the `TestMatch.testMatchFit2JAC` error:
     - `madl_plot.mad:100: couldn't delete TMP_MAD_PLOT : Directory not empty`
   - Verified with a targeted rerun of `TestMatch.testMatchFit2JAC`.

Remaining failures not fixed
- The normal `-e` run still stops first in `tests/utests/match.mad`.
- These no longer look like low-risk mechanical regressions; they are optimizer-behavior mismatches:
  - solver status mismatches such as `XTOL` vs `FTOL`, `FTOL` vs `MAXCALL`, `FMIN` vs `RUNNING`
  - iteration/count mismatches such as `2574` vs `2315`, `2539` vs `2142`, `404` vs `1032`
  - very tight numerical expectation failures at around `1e-13` with `1e-16` margins
  - one `match` error caused by an undeclared global in the test:
    - `TestMatch.testMatchFuncJAC6`
    - warning points to `./match.mad:911`
    - undeclared variable: `testMatchFunc6`
  - the log also shows:
    - `mad_nlopt.c:140: NLOPT config error 'INVALID_ARGS': too many equality constraints.`

- With `TestMatch` excluded, the remaining blockers are:
  - `TestMatrixFun.testMap`
  - `TestMatrixFun.testMap2`
  - `TestMatrixLapack.testPcacnd`:
    - hard segfault in the LAPACK path
- `testMap` and `testMap2` both pass in isolation and only fail in the full run, so they currently look like a shared-state/order issue rather than an obvious localized regression.
- I did not touch the `Pcacnd` segfault because that is well beyond the “obvious fix only” boundary.

- With `TestMatch`, `TestMatrixFun.testMap`, `TestMatrixFun.testMap2`, and `TestMatrixLapack.testPcacnd` excluded, the current sequence blockers are:
  - `TestSequence.testInitElemFromPos`
    - runtime error from `madl_sequence.mad:92`
    - `invalid element position at s = 3m [3m] (negative drift -1m) in sequence 'seq' for element 'nxt' at index 4`
    - excluding this test reaches the same block again:
      - `TestSequence.testInitElemFromPosWithName`
      - `madl_sequence.mad:92: invalid element position at s = 0m [0m] (negative drift -2m) in sequence 'seq' for element 'name2' at index 3`
    - `TestSequence.testInitElemFromPosInvalid` also now reports a different negative-drift message (`-2m` instead of `-1m`)
  - `TestSequence.testForeachTableNot`
    - current packed-form `default=true/"none"` behavior differs from the old expectation
  - `TestSequence.testDeselectTable`
  - `TestSequence.testDeselectDefaultFlag`
  - `TestSequence.testFilterTable`
    - these three look like a shared selection/filter default/range semantic shift rather than isolated typos
  - `TestSequence.testDumpseqFileName`
  - `TestSequence.testDumpseqStream`
  - `TestSequence.testDumpseqStdout`
  - `TestSequence.testDumpseqUninitializedLength`
    - reference-output token counts drifted (`57 -> 51`, `66 -> 59`, `12 -> 11`)
    - these may be fixture updates, but I did not change them blindly because they sit next to the broader selection/init regressions in the same suite

- Current error ladder after the simple fixes:
  - `TestMTable.testWriteCol`
    - `madl_mtable.mad:1485: unable to open MTable file in write mode`
    - only reproduces in the full unskipped run; it passes in isolation and in an isolated `TestMTable` run
    - this looks like leaked global/file state, not a localized `mtable:write()` regression
  - `TestMatrixLapack.testPcacnd`
    - hard segfault (`SIGSEGV`)
  - `TestSequence.testInitElemFromPos`
    - runtime error from `madl_sequence.mad:92`
    - reproducible once the matrix crash is skipped
  - I did not change any of these three because they are not obvious one-line fixes

Why I stopped here
- The remaining `match` failures are tied to optimizer convergence/status behavior.
- The newly exposed `sequence` failures are grouped around selection/filter semantics, dump output, and init-position behavior.
- Changing code or tests there without a clearer intended contract would be high risk and likely paper over a real solver/configuration issue.

Useful logs
- Latest early-stop log:
  - `tests/utests/full-test-continue.log`
- Latest error-only logs:
  - `tests/utests/error-pass-after-plot-fix.log`
  - `tests/utests/error-pass-x-writecol.log`
  - `tests/utests/error-pass-after-plot-fix-x-matrix.log`
  - `tests/utests/error-pass-deeper.log`
- Latest skip-`match` log:
  - `tests/utests/full-test-skip-match-after.log`
- Latest deeper skip log:
  - `tests/utests/full-test-skip-more.log`
- Latest isolated `mtable` log:
  - `tests/utests/mtable-only-after.log`
- Earlier logs kept during the pass:
  - `tests/utests/full-test.log`
  - `tests/utests/full-test-after.log`
  - `tests/utests/full-test-nonstop.log`
  - `tests/utests/full-test-skip-match.log`

Residual noise
- Many targeted runs still print:
  - `finalizer failed: madl_gutil.mad:74: assertion failed!`
- I did not isolate that separately because it did not block the focused fixes above.
